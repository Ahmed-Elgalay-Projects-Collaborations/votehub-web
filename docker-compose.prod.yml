services:
  web:
    build:
      context: .
      target: production
      args:
        VITE_API_URL: ${VITE_API_URL:-/api/v1}
    container_name: votehub-web-prod
    ports:
      - "8080:8080"
    restart: unless-stopped
